import React from 'react';

const TestReact = () => {
  return (
    <div>
      <button onClick={() => console.log('Clicked')}>
        Press me for console log
      </button>
    </div>
  );
};

export default TestReact;
